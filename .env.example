# Local demo API settings only.
# Do not commit real secrets or local credential files.
# This project does not include live ERP/Odoo/MCP/SAP integration.

RMC_API_BUNDLE_ROOT=/tmp/rmc_api_bundles
RMC_API_REGISTRY_DB=/tmp/rmc_api_bundles/demo_bundle_runs.sqlite
